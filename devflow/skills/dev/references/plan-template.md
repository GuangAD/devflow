# <特性名> 开发计划

> 设计依据:./design.md(已批准);实现规格:./spec.md(已批准,含接口组与用例表)
> 状态:待批准(门 2 批准后改为:已批准;全部完成后改为:已完成)

## 全局约束

从 design.md「技术栈与依赖约束」节逐字复制的要求,每个任务隐式继承:技术栈与版本下限、命名规则、平台要求、禁用项。

TDD waiver 如可预判(如渲染/纯配置任务段),在此预登记:waiver 范围 + 验收替代方式;随门 2 一并批准,执行期逐任务仍须在开发记录留痕。

## 任务清单

<!-- 进度标记:完成一个勾一个并回填 commit 短哈希;恢复时从第一个未勾选处继续 -->

- [ ] 任务 1:<名称>(commit: ____ )
- [ ] 任务 2:<名称>(commit: ____ )

## 任务详情

### 任务 1:<名称>

- **端到端行为**:从用户视角描述这个任务做完后能演示/验证什么。不写"修改某文件某行"式的施工清单。
- **涉及**:模块/文件级范围(新建、修改、测试各列一行)。
- **编码方向**:实现要点、复用什么、注意什么。
- **接口组**:本任务从红到绿的 spec.md seam 条目(如 `<模块>.<函数>`、`<模块>.<函数>`),以及消费的非 seam 导出。
- **数据结构落地**:本任务落地的部分,与 spec.md 对应节的关系。
- **验收标准**(机检项与目测项分开列,机检项注明命令与判定;数值预算必须机检;门 2 时预判哪些机检项在本机不可达——平台差异、权限、环境缺失——预先标注,避免执行期临时降标):
  - [ ] 标准 1(可验证)
  - [ ] 标准 2
- **依赖**:无 | 依赖任务 N 的 <接口名>(硬依赖:缺了它就做不了,用于决定派发顺序)。仅属顺序偏好的不必写在此处,在门 2 给推荐排序即可。

### 任务 2:…

## 开发记录

<!-- 每完成一个任务追加一块:首行是 ledger 行(任务 N | 状态 | commit),字段缩进成子行——resume 按标签读、人按行读。字段与义务:
- BASE 短哈希(必有):实现开始时的 HEAD,回退协议的 diff 基线
- 评审(必有):发现与处置;为缺陷补写的 pinned case 在此注明 mutation check(反向验证)结果(见 tdd 技能)
- waiver(有才写):已在全局约束预登记的注明「预登记」
- 变更记录(动过 spec.md 才写):注明「变更记录已登记」
- 结论(必有):下一步的会话或下游任务用得上的 1-3 句(踩过的坑、隐含约定、未落文档的推理),每条一行;无则写「无」——跨会话唯一的软依赖通道
- 备注(有才写)
空字段一律省略,不写「waiver:无」之类占位(评审与结论除外)。
坏:任务 2 | BASE 5dd6d24 | 完成 | 评审:初审 1 C(...)+1 I(...)→ 修复后 re-review 通过 | waiver:无 | 结论:①…②…③… | commit 35e4a86 ——一行 300 字,人无法扫读。
好:如下块格式。 -->

- 任务 1 | 完成 | commit abc1234
  - BASE:a1b2c3d
  - 评审:无发现
  - 结论:无
- 任务 2 | 完成 | commit def5678
  - BASE:b2c3d4e
  - 评审:Important 1 条已修复(re-review 通过);Minor 1 条记录在案(命名)
  - waiver:纯文案(预登记)
  - 结论:
    1. 图表 tooltip 必须走 tooltipText,直接拼接会在 dist 后被覆盖
    2. <第二条,没有则整条「结论」只留一行>
