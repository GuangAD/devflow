# CONTEXT — devflow 领域词汇表

本文件定义 devflow 各术语在我们这里的含义与边界。纯词汇表,不含实现细节——流程契约见 `devflow/skills/`,中英对照检索见 `devflow/README.md` 的术语表,决策理由见 `docs/adr/`。

## 轻流程(light track)

devflow 三种流程分层之一,零门:不设确认门,直接执行后汇报。适用信号——修 bug、调文案、改配置、重构既有函数、`docs/plans/` 下已有计划中的对应任务。

反义/易混淆:轻不等于改动小或省纪律。debugging(根因先行)、tdd、verification 在轻流程里照常适用,省掉的只是门。改 5 行配置可能判重(若它动数据结构),删 200 行死代码可能判轻。

## 重流程(heavy track)

devflow 三种流程分层之一,含 design.md + spec.md + plan.md 三件套与三道确认门。适用信号——新功能/新模块/新项目、需要改动数据结构、预计新建文件较多、用户说"做个/加个/实现一个"、该领域尚无设计文档覆盖。

反义/易混淆:判据不是改动大小,而是**有没有设计文档未覆盖的新决策要谈**。门 1 要求 design tree 无含糊分支——这正是巨流程存在的理由。

## 巨流程(huge track)

devflow 三种流程分层之一。不直接进重流程,先走 `/dev-map` 建决策地图,把"说不 sharp"的区域变成已定决策后再汇入重流程。判据两条——一次访谈装不下(design tree 一轮收不住,预计跨多个会话才谈得清);存在"连问题都说不 sharp"的区域(不是缺答案,是缺问题)。

反义/易混淆:**判巨优先于判重**——先看是不是一次访谈装不下,再谈轻重。design tree 一轮能收、没有说不 sharp 的区域,那是"重"不是"巨"。

## 三道门(Gate 1/2/3)

重流程上仅有的三个人工确认点。门 1 守方向(批准 design.md 与 spec.md 骨架)、门 2 守路径(批准用例详版与 plan.md)、门 3 守结果(验收最终报告)。门之间 agent 自治。

反义/易混淆:门的数量随仪式缩放——轻流程零门,巨流程期间由地图的 ticket 承担探路、同样不开门。门要人做判断(批设计、批计划、验收);任务边界换会话之类的机械动作不算门。

## 升级棘轮(ratchet)

分类结果只可向更自律的一侧升级:轻→重→巨。中途发现实为设计级改动(如修 bug 时发现根因在数据结构)必须停下、说明、转流程。

反义/易混淆:不可反向降。理由是误判代价不对称——重判轻是在方向未定时开工,返工成本按 design tree 规模放大;轻判重只是多花一轮访谈时间。

## 决策地图(map)

巨流程的产物,单个 Markdown 文件 `docs/maps/<YYYY-MM-DD>-<slug>/map.md`,进 git,五节:destination / 备注 / 已定决策 / fog / out of scope。

反义/易混淆:它是**索引不是仓库**——每条决策只存在它自己的 ticket 里,地图只存一行 gist,不复述结论。地图**只产决策不产代码**;想动手的冲动通常是信号:已走到地图边缘,该交接给 `/dev` 了。

## destination(目的地)

地图第一节,一两行,钉死这次要走到哪,从而定死范围。每个会话开工前先读它再选 ticket——它决定某张 ticket 该不该存在。

反义/易混淆:destination 定的是**范围**,不是**完成标准**。它在整张图的生命周期内不变,除非 destination 被重画——那时它是新的一次努力,不是续集。

## ticket(票)

决策地图里的条目,一行一张,状态用 checkbox,解完回填 commit 哈希(与 plan.md 同一套记账法);问题全文与结论写在条目下方。四型——调研(AFK,派 subagent)、原型(HITL,提高讨论保真度)、访谈(HITL,默认类型)、任务(本身不是决策,靠"解锁某个决策"挣到位置)。

反义/易混淆:ticket 与 plan.md 的任务不是一回事——ticket 解的是**决策**,任务交付的是**代码**。提到 ticket 就说名字,不说编号:一面墙的 `#42 #43` 读不了。

## fog(雾)

地图里"还说不 sharp"的区域。

反义/易混淆:**fog 与 ticket 的判据是——你现在能不能把这个问题说精确,而不是你现在能不能回答它。** 能说精确就开 ticket(哪怕它还被别的 ticket 阻塞着),还不能说 sharp 就留在 fog 里。它与 design.md 的「开放问题」节不是一回事:后者是"已知但还没定",fog 是"知道那儿有东西、但连问题都还说不出"。别把 fog 预切成 ticket 大小的块——一块 fog 等 frontier 推到它时可能毕业成几张 ticket,也可能一张都不成。

## frontier

本仓库有**两个不同含义**,按语境区分。

在 grilling/访谈语境:前置条件已落定、现在就能问的问题集;设计树按轮次推进,每轮抛出当前 frontier 上的全部问题。

在决策地图语境:当前无阻塞、可解的 ticket 集合;走地图时默认取 frontier 第一张(用户点名则用点名的)。

反义/易混淆:地图语境的 frontier 与 ready set 属**不同层**——frontier 是地图层的探路单位(解决策),ready set 是开发层的实现单位(写代码)。两者都是"当前能动的集合",不要互换使用。

## out of scope(越界)

地图里超出 destination、这次明确不做的部分。

反义/易混淆:**永不毕业**,除非 destination 被重画。已开的 ticket 若被发现其实在 destination 之外就关掉它(关掉的确定不在 frontier 上),在「out of scope」节留一行 gist 加原因并链过去——它不进「已定决策」,因为那是记录实际走过的路,范围边界不是路上的一步。

## tracer-bullet slice(曳光弹切片)

阶段 2 的切任务方式:每个任务纵向贯穿各层、可独立验收、单个会话可容纳。一个任务 = spec.md 里的一组 seam 条目从红到绿。

反义/易混淆:与 vertical slice 同源,是同一件事的不同粒度——任务级的贯穿各层,测试级的是一次一条行为。

## vertical slice / horizontal slice

vertical slice 是纵向的一次一条行为:一个测试对应一点实现,让每个测试回应上一个切片的发现。horizontal slice 是一口气写完所有测试再写全部实现,tdd 明令禁止——那验证的全是想象中的行为。

反义/易混淆:历史上本仓库混用过「纵向切片」与「垂直切片」两个中文词指同一个概念(vertical slice),2026-09-16 术语英文化时统一为 vertical slice。所以旧文档里出现的这两个词是同一件事,不是两种切片。

## ready set(解锁集)

所有硬依赖已完成、且自身未完成的任务集。每次准备做下一个任务前先重算它,再从中取一个派发。

反义/易混淆:**与 frontier 不同层**(见 frontier 条)。plan.md 的「依赖」字段只用于决定顺序,不必穷举隐性耦合——串行执行下,上游提交后代码已真实存在,下游读代码即可拿到接口。

## implementer subagent(实现者)

阶段 3 的优先执行形态:按 `references/implementer-prompt.md` 填简报并派发,实现留在工作区。四条边界——不提交、不改 design/spec/plan、不评审自己、不派 subagent;发现设计空白即停手报告。

反义/易混淆:环境无 subagent 能力时降级为**会话内实现**(fallback),此时 controller 自己写代码;两者的差异只在"谁写代码",纪律(tdd、verification)不变。

## reviewer / self-review fallback

reviewer 是独立上下文的评审者,**不接触 controller 的会话历史**——这正是它独立的原因;它只按派发模板给出的简报、接口规格、设计约束、差异文件与实现者报告工作。环境无 subagent 能力时降级为 self-review,并须**标注"非独立"**。

反义/易混淆:self-review 是知情降级,不是等价替代——它的结论不得当作独立评审通过。devflow 的核心原则是**implementer 不得 self-review 放行**。

## controller(控制器)

dev 主流程所在的会话。负责填派发简报、**自己重跑验证**(subagent 回报的测试结果不算数)、处置评审发现、提交、勾选 plan.md。

反义/易混淆:只有 controller 有提交权与门权;subagent 一律不提交。派发 diff 走文件(`.review/<任务号>.patch`)而不灌进对话上下文。

## seam(接缝)

spec.md 里需要与模块外交互的接口边界。每条 seam 条目必须有四件套——签名 / 后置条件 / 副作用与可重入性 / 错误行为,且至少 1 条 pinned case。接口面穷举清单逐行标注每个公共导出是否 seam。

反义/易混淆:seam 条目缺四件套之一、或穷举清单不完整,视同开放问题非空,**不得呈报门 1**。非 seam 模块不进四件套。

## pinned case / mutation check / waiver

pinned case(钉住用例)是为**已发现的缺陷**补写、钉住其行为防复发的用例。mutation check(反向验证)是删掉或改坏被钉住的那处赋值、确认**这条**用例转红、再改回来,以证明它有鉴别力。waiver 是 TDD 豁免,须留痕写明理由,已在 plan.md 预登记的注明预登记。

反义/易混淆:mutation check 是 devflow **自创词**(两个上游项目均无),来自 pi-w 实证中评审处置长出的做法;它是手工版、定向版的变异测试,只在"刚修完缺陷、刚写完钉住用例"这一刻做一次,不铺开到常规切片。它与"亲眼看红"是一体的两端——TDD 保这条测试不是一开始就绿,反向验证保它没有退化成永远绿。

## 上下文预算(context budget) / 传感器与策略分离

上下文预算管理 controller 会话的上下文增长:每个任务提交后调 `context_usage` 工具读一次占比,**≥ 45% 就在任务边界提示换会话**;工具不可用时退回三条可数信号(同一节文档第三次重读 / 最近两个任务都需回翻前文 / 已记不清早前决定),底线是完成 4 个任务后不论感觉如何都检查一次。传感器与策略分离指该工具所在的扩展**只报数、不判阈值**——阈值与处置全部写在 dev 技能里,改线不必动代码、不必重装包。

反义/易混淆:读数与阈值不在同一处是有意的。**"感觉挤了"不是判据**——模型无法感知自身的上下文占用,而上下文压力最先损害的恰恰就是"感觉到自己挤了"这个能力。
